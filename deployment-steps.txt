## Save pass:


## Run from terminal:
#If in case error for first line 
echo $DOCKER_PASSWORD | docker login mlopsdg.azurecr.io --username $DOCKER_USERNAME --password-stdin

docker build -t geminiappdesc.azurecr.io/geminiappdesc:latest .

docker login geminiappdesc.azurecr.io

docker push geminiappdesc.azurecr.io/geminiappdesc:latest


## Deployment Steps:

1. Build the Docker image of the Source Code
2. Push the Docker image to Container Registry
3. Launch the Web App Server in Azure 
4. Pull the Docker image from the container registry to Web App server and run 